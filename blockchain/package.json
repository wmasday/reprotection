{
  "name": "blockchain",
  "version": "1.0.0",
  "description": "Blockchain blacklist keyword management system",
  "main": "api.js",
  "scripts": {
    "compile": "npx hardhat compile",
    "deploy": "npx hardhat run scripts/deploy.js --network localhost",
    "node": "npx hardhat node",
    "api": "node api.js",
    "start": "npm run compile && npm run deploy && npm run api",
    "test": "npx hardhat test"
  },
  "keywords": [
    "blockchain",
    "solidity",
    "ethereum",
    "blacklist"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "ethers": "^6.8.1",
    "express": "^4.21.2",
    "hardhat": "^2.25.0"
  }
}
